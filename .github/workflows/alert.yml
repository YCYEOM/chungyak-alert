name: chungyak-alert

on:
  repository_dispatch:      # Cloudflare Worker cron이 발화 — GitHub schedule은 KST 아침(UTC 자정 피크)에 드롭이 잦아 폐기
    types: [run-alert]      # client_payload.light: true면 무순위 라이트 폴링
  workflow_dispatch:        # 수동 실행 버튼

permissions:
  contents: write           # seen.json 커밋용

concurrency:
  group: chungyak-alert     # 동시 실행 방지 (seen.json 충돌 예방)

jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.12'

      - name: Install deps
        run: pip install -r requirements.txt

      - name: Run alert bot
        env:
          SERVICE_KEY: ${{ secrets.SERVICE_KEY }}
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
          LIGHT_MODE: ${{ github.event.client_payload.light && '1' || '' }}
        run: python chungyak_alert.py

      - name: Persist seen.json
        run: |
          git config user.name "chungyak-bot"
          git config user.email "actions@users.noreply.github.com"
          git add seen.json
          if ! git diff --cached --quiet; then
            git commit -m "chore: update seen.json"
            git pull --rebase origin main   # 실행 중 Worker가 subs.json을 커밋했을 수 있음
            git push
          fi

      - name: Notify on failure
        if: failure()
        env:
          TELEGRAM_TOKEN: ${{ secrets.TELEGRAM_TOKEN }}
          TELEGRAM_CHAT_ID: ${{ secrets.TELEGRAM_CHAT_ID }}
        run: |
          IFS=',' read -ra IDS <<< "$TELEGRAM_CHAT_ID"
          for id in "${IDS[@]}"; do
            curl -s --max-time 15 "https://api.telegram.org/bot${TELEGRAM_TOKEN}/sendMessage" \
              -d chat_id="${id// /}" \
              -d text="🚨 청약 알림봇 실행 실패 — 로그 확인: https://github.com/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
              > /dev/null || true
          done
